# Fixture Data

Initial MoaView data is fixture-only and must not be treated as scraped production data.

- `works.json`: searchable works by title and author.
- `offers.json`: platform offers with required price fields, source URL, and last verified time.
- `promotions.json`: platform event metadata.
- `coupons.json`: coupon metadata labeled as expected prices when user action is required.
